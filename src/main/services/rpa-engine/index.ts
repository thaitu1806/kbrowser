/**
 * RPA Engine Service
 *
 * Bộ tự động hóa kéo-thả: thực thi kịch bản RPA tuần tự
 * (navigate, click, type, wait, scroll, screenshot),
 * quản lý mẫu tự động hóa và xử lý lỗi.
 */

export { RPAEngine } from './rpa-engine';
export type { ActionExecutorFn } from './rpa-engine';
