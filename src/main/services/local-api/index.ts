/**
 * Local API Server Service
 *
 * HTTP server trên cổng 5015 cho tích hợp automation bên ngoài:
 * xác thực API key, mở/đóng hồ sơ, danh sách hồ sơ.
 */

export { LocalAPIServer } from './local-api-server';
